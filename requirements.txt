# dbt
dbt-databricks==1.8.3

# Text-to-SQL chat app (app/) — generation runs on Databricks (ai_query),
# these are only needed to run the local FastAPI server and connect to the
# SQL Warehouse.
fastapi==0.115.0
uvicorn==0.30.6
databricks-sql-connector
